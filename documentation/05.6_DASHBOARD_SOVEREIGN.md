# 👑 5.6 Dashboard: Estadísticas Soberanas (`dashboard.py`)

> **Objetivo**: Mostrar el valor de los datos del cliente. Monetización del conocimiento.
> **Sección**: Sovereign Memory

---

## 1. `get_sovereign_stats`
```python
@router.get("/sovereign/stats")
```
*   **Concepto**: "Knowledge as an Asset".
*   Si un tenant permite compartir sus respuestas cacheadas (anonimizadas) con el ecosistema, recibe micro-pagos cada vez que otro tenant usa esa respuesta (Cache Hit global).

### Cálculo de Ganancias
```python
total_earnings = sum(abs(item['amount'])) for item in transactions if amount < 0
```
*   AgentShield registra estas transacciones con **monto negativo** (crédito) en el libro mayor (`pending_transactions_log`).
*   Este endpoint suma todos esos créditos para mostrar "Cuánto ha ganado tu conocimiento hoy".
*   Incentiva a las empresas a contribuir al "Cerebro Global" en lugar de ser silos cerrados.
