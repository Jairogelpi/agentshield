# Dockerfile (Fast Wrapper for Render)
# Esta imagen simplemente descarga la versión ya compilada por GitHub Actions.
# Esto hace que el build en Render dure < 10 segundos.
FROM ghcr.io/jairogelpi/agentshield:latest